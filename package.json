{
  "name": "TODO",
  "description": "TODO",
  "version": "0.0.1",
  "scripts": {
    "dev": "run-p dev:tailwind dev:sapper",
    "dev:sapper": "sapper dev",
    "dev:tailwind": "postcss src/tailwind.css -o static/tailwind-compiled.css -w",
    "build": "yarn build:tailwind && NODE_ENV=production sapper build",
    "build:tailwind": "NODE_ENV=production postcss src/tailwind.css -o static/tailwind-compiled.css",
    "export": "yarn build:tailwind && NODE_ENV=production sapper export",
    "start": "node __sapper__/build",
    "cy:run": "cypress run",
    "cy:open": "cypress open",
    "test": "run-p --race dev cy:run"
  },
  "dependencies": {
    "compression": "^1.7.1",
    "polka": "next",
    "sirv": "^0.4.0"
  },
  "devDependencies": {
    "@fullhuman/postcss-purgecss": "^2.1.2",
    "npm-run-all": "^4.1.5",
    "postcss-cli": "^7.1.0",
    "prettier": "^2.0.4",
    "prettier-plugin-svelte": "^0.7.0",
    "sapper": "^0.27.0",
    "svelte": "~3.19.0",
    "svelte-loader-hot": "^0.3.0",
    "svelte-preprocess": "^3.7.1",
    "tailwindcss": "^1.2.0",
    "webpack": "^4.7.0"
  }
}
